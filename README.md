# 爱赞美 (Praise Songs)

[![Gitee Repo](https://img.shields.io/badge/Gitee-praise--songs-red?logo=gitee)](https://gitee.com/jireh_rick/praise-songs)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

**爱赞美** 是一个现代化的基督教赞美诗歌资料库与演示管理平台。它集成了音乐试听、歌谱浏览、歌词投影、资料管理等功能，旨在为教会敬拜团、团契及个人提供一站式的赞美诗歌解决方案。

## ✨ 核心功能

### 🎵 音乐与资料库
- **多维浏览**: 支持按 **音乐人**、**专辑**、**歌单**、**歌谱** 等维度浏览资源。
- **在线试听**: 内置全功能播放器，支持列表播放、后台播放及无缝切歌。
- **歌谱查看**: 支持多版本歌谱（简谱、五线谱、和弦谱等）预览与下载。
- **LRC 歌词**: 播放时自动同步滚动歌词。

### 📽️ 演示与投影
- **专业演示模式**: 专为投影设计的全屏视图，支持自定义背景图片、字体大小和样式。
- **参数记忆**: 每首歌曲的演示配置（背景、字号等）会自动保存，下次使用无需重复设置。

### 📤 智能上传系统
- **元数据自动提取**: 上传 MP3 等音频时，自动识别 ID3 标签（标题、歌手、专辑）填充表单。
- **LRC 智能解析**: 自动解析 LRC 文件中的 `[ti:歌名]`、`[ar:歌手]`、`[al:专辑]` 标签。
- **智能防乱码**: 自动检测并修复 GBK/UTF-8 编码问题，解决中文乱码困扰。
- **批量与拖拽**: 支持多文件批量拖拽上传，自动分类处理（音频、视频、歌谱、歌词、封面）。

### 🛡️ 管理后台 (Admin Dashboard)
系统内置了完善的后台管理功能，管理员可通过点击右上角头像进入 **管理后台**：
- **用户管理**: 查看注册用户列表，修改用户角色（普通用户/管理员）及权限。
- **歌单审核**: 审核用户提交的公开歌单申请，维护平台内容质量。
- **数据管理**: 快捷访问 **Prisma Studio** 可视化数据库管理界面，对平台内的歌曲、专辑、音乐人信息进行统一维护。

## 💾 数据管理与查看

本项目使用 **SQLite** 作为数据库，配合 **Prisma ORM** 进行数据操作。我们提供了直观的方式来查看和管理底层数据。

### 使用 Prisma Studio
Prisma Studio 是一个可视化的数据库编辑器，允许你直接在浏览器中查看和编辑数据库记录。

1.  **启动 Studio**：
    在 `web` 目录下运行：
    ```bash
    npx prisma studio -p 5556
    ```
    （为了与管理后台集成，请使用端口 5556）

2.  **访问管理界面**：
    启动后，您可以直接点击 **管理后台** 中的 **“数据管理”** 卡片，或直接访问 [http://localhost:5556](http://localhost:5556)。
    > 注意：Prisma Studio 仅支持本地访问（localhost/127.0.0.1）。如果您在远程服务器部署，需要通过 SSH 隧道转发端口 5556。

### 数据库文件位置
- **数据库文件**: `data/db/dev.db` (SQLite 文件)
- **媒体资源**: `data/storage/` (存储上传的音频、图片等)

> 注意：`data` 目录包含了所有持久化数据，请定期备份该目录。

## 🚀 快速部署

### Docker 一键部署 (推荐)
无需配置环境，直接运行即可。

1.  **启动服务**
    ```bash
    docker-compose up -d --build
    ```
2.  **访问应用**
    - 前台页面: [http://localhost:3000](http://localhost:3000)
    - 数据库存储: `./data/db`
    - 文件存储: `./data/storage`

### 本地开发环境
1.  **安装依赖**
    ```bash
    cd web
    npm install
    ```
2.  **初始化数据库**
    ```bash
    npx prisma generate
    npx prisma db push
    ```
3.  **启动服务**
    ```bash
    npm run dev
    ```

## 📂 项目结构

```
.
├── web/                 # Next.js 前端应用核心代码
│   ├── app/             # App Router 路由
│   │   ├── admin/       # 管理后台页面 (用户管理、歌单审核、数据管理)
│   │   ├── api/         # 后端 API 接口
│   │   ├── upload/      # 上传页面
│   │   └── ...
│   ├── components/      # 通用 UI 组件
│   ├── lib/             # 核心库 (Prisma Client, Auth, Utils)
│   ├── prisma/          # 数据库模型定义 (schema.prisma)
│   └── public/          # 静态资源
├── data/                # 数据持久化目录 (Git Ignored)
│   ├── db/              # SQLite 数据库文件
│   └── storage/         # 媒体资源文件
├── Dockerfile           # Docker 构建配置
└── docker-compose.yml   # Docker 编排配置
```

## 🛠 技术栈

- **前端框架**: [Next.js 14](https://nextjs.org/) (App Router, React Server Components)
- **开发语言**: TypeScript
- **UI 组件库**: Tailwind CSS, Shadcn UI, Lucide React
- **数据库**: SQLite
- **ORM**: [Prisma](https://www.prisma.io/)
- **认证**: JWT (自定义实现)

## 📄 许可证

本项目采用 MIT 许可证。
